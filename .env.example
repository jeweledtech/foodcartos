# FoodCartOS Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# APPLICATION SETTINGS
# ===========================================
APP_ENV=development
APP_NAME=FoodCartOS
API_BASE_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Secret key for JWT signing (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-change-in-production

# ===========================================
# SUPABASE (Database + Auth)
# ===========================================
# Get these from your Supabase project settings
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# Direct database connection (for migrations)
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# ===========================================
# SQUARE (POS Integration)
# ===========================================
# Get these from Square Developer Dashboard
SQUARE_ACCESS_TOKEN=your-square-access-token
SQUARE_APPLICATION_ID=your-application-id
SQUARE_LOCATION_ID=your-default-location-id

# Webhook signature key (from Square webhook settings)
SQUARE_WEBHOOK_SIGNATURE_KEY=your-webhook-signature-key

# Environment: sandbox or production
SQUARE_ENVIRONMENT=sandbox

# ===========================================
# TWILIO (SMS)
# ===========================================
# Get these from Twilio Console
TWILIO_ACCOUNT_SID=your-account-sid
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Optional: Messaging Service SID (for higher volume)
TWILIO_MESSAGING_SERVICE_SID=

# ===========================================
# WEATHER API (Optional)
# ===========================================
# OpenWeatherMap API key
OPENWEATHER_API_KEY=your-openweather-api-key

# ===========================================
# N8N (Workflow Automation)
# ===========================================
# If using n8n cloud
N8N_WEBHOOK_BASE_URL=https://your-instance.app.n8n.cloud/webhook

# If self-hosting n8n
# N8N_WEBHOOK_BASE_URL=http://localhost:5678/webhook

# ===========================================
# STORAGE (Photos)
# ===========================================
# Supabase Storage is default, these are optional overrides

# For S3-compatible storage
# STORAGE_PROVIDER=s3
# S3_BUCKET=foodcartos-photos
# S3_REGION=us-west-2
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key

# ===========================================
# HARDWARE AGENT (Cart-side)
# ===========================================
# These are used by the Raspberry Pi agent

# API endpoint for the cart to connect to
AGENT_API_URL=https://api.foodcartos.com

# Hardware-specific
AGENT_HARDWARE_ID=  # Auto-generated on first run
AGENT_ORG_ID=       # Set during cart registration

# GPS settings
GPS_UPDATE_INTERVAL_SECONDS=300
GPS_GEOFENCE_RADIUS_METERS=100

# Photo settings
PHOTO_QUALITY=85
PHOTO_MAX_WIDTH=1920

# Sync settings
SYNC_INTERVAL_SECONDS=60
OFFLINE_QUEUE_MAX_SIZE=1000

# ===========================================
# DEVELOPMENT ONLY
# ===========================================
# Enable debug mode (never in production!)
DEBUG=true

# Disable SSL verification (local development only)
VERIFY_SSL=false

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=DEBUG
